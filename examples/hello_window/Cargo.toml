[package]
name = "hello_window"
description = "Oxide Core example - basic window creation"
version = "0.1.0"
edition = "2021"

[dependencies]
oxide_engine = { path = "../../crates/oxide_engine" }
wgpu.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true